// This function checks if a string includes a specific substring.

const str = "Hello, world!";
const substring = "world";
const includesSubstring = str.includes(substring);
console.log(includesSubstring); // Output: true
