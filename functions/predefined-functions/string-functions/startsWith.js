// The startsWith() method checks if a string starts with a specified substring
// useful in scenarios like validating prefixes, filtering data, or processing strings.

let str = `Hello world`;
let strStarts = str.startsWith(`H`,0);
console.log(strStarts);
