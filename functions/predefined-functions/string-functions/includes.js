// The includes() method checks if a string contains a specified substring
// useful in scenarios like searching, filtering, or validating input.

const str = `pineapple`;
const strIncludes = str.includes(`apple`);
console.log(strIncludes);
