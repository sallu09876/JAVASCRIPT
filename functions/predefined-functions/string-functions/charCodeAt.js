// The charCodeAt() method returns the Unicode of the character at a specified index in a string
// useful in scenarios like encoding, decoding, or character validation.

let str = "Hello, world!";
let code = str.charCodeAt(3);
console.log(code);