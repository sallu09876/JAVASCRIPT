// it extracts a part of a string and returns it as a new string, without modifying the original string.

let str = "Hello, world!";
// let substring = str.substr(0, 5);
// console.log(substring);
let substring = str.substring(7, 12);
console.log(substring);