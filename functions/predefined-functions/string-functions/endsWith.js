// The endsWith() method checks if a string ends with a specified substring
// useful in scenarios like validating file extensions, URLs, or specific string patterns.

let str = `Hello world`;
let strEnds = str.endsWith(`l`);
console.log(strEnds);
