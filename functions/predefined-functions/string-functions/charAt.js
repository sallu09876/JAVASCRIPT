// The charAt() method returns the character at a specified index in a string
// useful in scenarios like parsing strings, validating input, or extracting specific characters.

let str = "Hello, world!";
let char = str.charAt(0);
console.log(char);