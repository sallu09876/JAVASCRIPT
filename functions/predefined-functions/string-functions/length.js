// The length property returns the length of a string
// useful in scenarios like validating input length, processing strings, or formatting output.

let str = "Hello, world!";
let length = str.length;
console.log(length);