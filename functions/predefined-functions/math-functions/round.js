// The round() method rounds a number to the nearest integer
// useful in scenarios like rounding prices, scores, or measurements.

let num = 5.67;
let rounded = Math.round(num);
console.log(rounded);
