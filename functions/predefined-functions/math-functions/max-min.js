// The max() method returns the largest of zero or more numbers
// The min() method returns the smallest of zero or more numbers
// useful in scenarios like finding highest/lowest scores, prices, or measurements.

let max = Math.max(5, 10, 15);
let min = Math.min(5, 10, 15);
console.log(max);
console.log(min);